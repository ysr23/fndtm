$('i').html('<i class="icon-spinner icon-spin icon-large"></i>');
$('#todays_tasks').html('<%= j render "index" %>');
$(document).ready(function() {  
    // the id 775 below is not used for this kind of update
    // see the tasks#update for why
    $('.edit_area').editable('/tasks/775/update',{ 
      type     : 'textarea', 
      submit   : 'OK',
      placeholder : '',
      cols : '20',
      rows : '20', 
      id   : 'elementid',
      name : 'value',
      foo : 'bar'
      });
    }); 
// alert("Hi Ya!!");
